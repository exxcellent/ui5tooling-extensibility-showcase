specVersion: "1.0"
type: application
metadata:
  name: ui5tooling-customtask
resources:
  configuration:
    paths:
      webapp: dist
builder:
  customTasks:
    - name: ui5-task-transpile
      afterTask: replaceVersion
